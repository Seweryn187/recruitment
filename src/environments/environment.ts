/*  normally this file should be added to .gitignore,
    but in this case it is not to be able to download the finished project with environment set
*/

export const environment = {
  production: false,
};
